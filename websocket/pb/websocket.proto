syntax = "proto3";

option go_package = ".;pb";

package pb;

// Method
enum Method {
  // UNKNOWN
  UNKNOWN = 0;
  // PING 心跳
  PING = 1;
  // SUBSCRIBE 消息订阅
  SUBSCRIBE = 2;
  // UNSUBSCRIBE 取消订阅
  UNSUBSCRIBE = 3;
  // BROADCAST 广播
  BROADCAST = 4;
  // DISCONNECT 断开
  DISCONNECT = 5;
}